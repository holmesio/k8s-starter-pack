[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi==0.115.0",
    "prometheus-client==0.20.0",
    "redis==5.0.8",
    "uvicorn==0.30.6",
]
